# Services

Generated by Struktur (Nunjucks) at {{ buildContext.timestamp }}

## Overview

This stack contains {{ instances | length }} service(s).

## Services

{% for instance in instances %}
{% if instance.class == "web_service" %}
### {{ instance.name }}

- **ID**: `{{ instance.id }}`
- **Description**: {{ instance.description }}
- **URL**: {{ instance.$aspects.web_service.protocol }}://{{ instance.$aspects.web_service.hostname }}:{{ instance.$aspects.web_service.port }}
- **Workers**: {{ instance.$aspects.web_service.workers }}
{% if instance.$aspects.web_service.ssl_enabled %}
- **SSL**: Enabled
  - Certificate: `{{ instance.$aspects.web_service.ssl_cert }}`
  - Key: `{{ instance.$aspects.web_service.ssl_key }}`
{% else %}
- **SSL**: Disabled
{% endif %}
- **Domains**: {{ instance.domains | join(", ") }}

{% endif %}
{% endfor %}

## Deployment

```bash
# Start all services
docker-compose up -d

# Check status
docker-compose ps

# View logs
docker-compose logs -f

# Stop all services
docker-compose down
```
