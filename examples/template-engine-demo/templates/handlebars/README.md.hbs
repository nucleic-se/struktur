# Services

Generated by Struktur (Handlebars) at {{$metadata.timestamp}}

## Overview

This stack contains {{length $instances}} service(s).

## Services

{{#each (where $instances "$class" "web_service")}}
### {{name}}

- **ID**: `{{$id}}`
- **Description**: {{description}}
- **URL**: {{$aspects.aspect_web_service.protocol}}://{{$aspects.aspect_web_service.hostname}}:{{$aspects.aspect_web_service.port}}
- **Workers**: {{$aspects.aspect_web_service.workers}}
{{#if $aspects.aspect_web_service.ssl_enabled}}
- **SSL**: Enabled
  - Certificate: `{{$aspects.aspect_web_service.ssl_cert}}`
  - Key: `{{$aspects.aspect_web_service.ssl_key}}`
{{else}}
- **SSL**: Disabled
{{/if}}
- **Domains**: {{#each domains}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}

{{/each}}

## Deployment

```bash
# Start all services
docker-compose up -d

# Check status
docker-compose ps

# View logs
docker-compose logs -f

# Stop all services
docker-compose down
```
