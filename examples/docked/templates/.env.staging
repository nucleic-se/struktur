# Staging Environment Configuration
# Deploy this to staging servers with proper secrets management

# Environment
NODE_ENV=staging
LOG_LEVEL=info

# Database (use managed database service in real staging)
POSTGRES_DB=docked_app_staging
POSTGRES_USER=docked_staging
POSTGRES_PASSWORD=REPLACE_WITH_VAULT_SECRET

# Redis
REDIS_PASSWORD=REPLACE_WITH_VAULT_SECRET

# API
API_SECRET=REPLACE_WITH_VAULT_SECRET
