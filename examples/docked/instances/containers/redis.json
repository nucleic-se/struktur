{
  "id": "redis",
  "class": "docked_container",
  "description": "Redis in-memory data store",
  "domains": ["application"],
  "aspects": {
    "docker_container": {
      "image": "redis:alpine",
      "description": "Redis in-memory data store",
      "networks": ["docked"],
      "restart_policy": "unless-stopped"
    }
  }
}
