{
  "aspect": "docker_network",
  "schema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "description": "Docker network configuration",
    "additionalProperties": true,
    "properties": {
      "driver": {
        "type": "string",
        "description": "Docker network driver (bridge, overlay, etc.)"
      }
    }
  }
}
