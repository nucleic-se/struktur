{
  "$aspect": "aspect_terraform",
  "$schema": {
    "type": "object",
    "properties": {
      "enabled": {
        "type": "boolean",
        "description": "Whether this resource is managed by Terraform"
      }
    },
    "required": [
      "enabled"
    ]
  }
}
