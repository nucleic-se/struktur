{
  "class": "compute_node",
  "parent": "infrastructure_entity",
  "pretty_name": "Compute Node",
  "$uses_aspects": ["compute_node"],
  "schema": {
    "type": "object",
    "description": "Base class for compute infrastructure (servers, VMs, containers)",
    "additionalProperties": true
  }
}
